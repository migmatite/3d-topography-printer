# downloader.py - Code for downloading topography data

def download_topography_data(bbox):
    """Downloads topography data for a specified bounding box (bbox).
    bbox: tuple -> (min_lat, min_lon, max_lat, max_lon)
    Returns path to downloaded data file."""
    # Step 1: Use an API (e.g., USGS, OpenTopography) to fetch DEM data
    # Step 2: Save data to 'data/raw/' directory
    # Step 3: Return the file path of the downloaded data
    pass
